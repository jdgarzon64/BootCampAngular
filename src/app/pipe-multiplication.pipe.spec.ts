import { PipeMultiplicationPipe } from './pipe-multiplication.pipe';

describe('PipeMultiplicationPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMultiplicationPipe();
    expect(pipe).toBeTruthy();
  });
});
